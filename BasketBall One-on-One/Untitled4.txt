for(int i=0;i<=ln.length();i += 2){
        letter[i] = ln[i];
        cout << letter[i] << "\n";
        for(int j=1;j<=ln.length(); j+=2){
            number[j] = ln[j];
            cout << number[j] << "\n";
        }
    }
